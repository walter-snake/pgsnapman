#!/bin/bash

# Get single jobs that have not run
# select * from vw_dumpjob_worker_instance where jobtype = 'SINGLE' and id not in (select jobid from pgsnap_singlerun where jobclass = 'DUMP');


exit 0
